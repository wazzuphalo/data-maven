export default function Home() {
  return (
    <div className="mx-auto max-w-(--container-page) px-6 py-24">
      <h1 className="text-hero font-heading font-semibold">
        Home page under construction.
      </h1>
      <p className="mt-4 max-w-(--container-content) text-body-lg text-text-muted">
        Layout shell, design tokens, and dark mode are wired up. Full home
        page content lands in the next build step.
      </p>
    </div>
  );
}
